name: zynost_client_public_reference
description: Public, production-safe reference for Zynost client state boundaries and BNB checkout orchestration.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
